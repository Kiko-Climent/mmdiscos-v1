/**
 * Slider de releases modular (Three.js + paneles HTML).
 * Implementación en `src/components/FinalReleases/` para escalar sin un único archivo enorme.
 */
export { default } from "../FinalReleases";
